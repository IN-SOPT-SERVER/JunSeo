const arr1 = ["정준서", "쌀국수", 23, true];
const arr2 = Array("ㄱ", "ㄴ", "ㄷ", "ㄹ");

//배열의 내장 메소드들이 있음 map, concat, filter 등등,,
arr1.map((item) => console.log("item: ", item));
arr1.map((item) => console.log(`item: ${item}`));
